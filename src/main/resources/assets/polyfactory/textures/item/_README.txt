ITEM TEXTURES — Place your .png files here (16x16 recommended).

Expected files:
  storage_cable.png         — Cable item icon (if different from block texture)

Most block items will just reference the block model, so you may not
need many files here. Items that are NOT blocks (e.g. a wireless
interface item) would go here.

Delete this file once you've added your textures.
