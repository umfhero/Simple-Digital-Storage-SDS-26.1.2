BLOCKSTATES — JSON blockstate definition files go here (auto-generated by code).

These will be created programmatically during development. You do NOT
need to add anything here manually.

Examples of files that will be created:
  storage_hub.json
  storage_cable.json          (will have connectivity variants)
  storage_interface.json      (will have facing direction variants)
